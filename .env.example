# Environment Variables Configuration Template
# Copy this file to .env and fill in your actual values
# Never commit the .env file with real secrets

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Add your security-related environment variables here

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# CORS allowed origins for cross-origin requests
# Comma-separated list of allowed origins
# Default: http://localhost:3000 (for React frontend)
# Example for multiple origins: CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
CORS_ALLOWED_ORIGINS=http://localhost:3000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL database configuration
# These should match your compose.yml values
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=hotel-management-service

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# Setting Environment Variables:
#
# Spring Boot automatically loads .env file from project root via:
# spring.config.import: optional:file:.env[.properties]
#
# 1. Recommended Local Development:
#    - Copy this file to .env and fill in your values
#    - Run: ./mvnw spring-boot:run
#    - Spring Boot automatically loads all variables from .env
#
# 2. IDE Development:
#    - IDE automatically reads .env file (Spring Boot integration)
#    - Or set environment variables in IDE run configuration
#
# 3. Docker Compose (when adding app service):
#    - Add environment section to finance-api service
#    - Use .env file automatically with docker compose up
#
# 4. CI/CD and Production:
#    - Set environment variables directly in your deployment platform
#    - Use vault or secret manager for sensitive values

